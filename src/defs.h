/*
 * defs.h provides handy definitions for types which can be tedious to type
 * repeatedly.
 * */

// container shorthands
#define Vec   std::vector
#define Map   std::map
// pointer shorthands
#define s_ptr std::shared_ptr
#define u_ptr std::unique_ptr
// types
typedef unsigned short int u_short;
